---
title: Первый запуск
description: Что происходит при первом запуске
---

# Первый запуск

## Splash-экран

При старте лаунчер показывает splash-экран, пока проверяет наличие сохранённого токена. Если токен есть и валиден — переходит на Home. Если нет — на Auth.

## Авторизация

Вводишь адрес сервера (если не задан в конфиге), логин и пароль. Лаунчер отправляет запрос через текущий адаптер и получает `AuthToken`.

Токен сохраняется в `FlutterSecureStorage` (на Windows — через DPAPI). При следующем запуске авторизация не нужна.

## Главный экран

После логина видишь список серверов с онлайном. Кликаешь на сервер — открывается его страница с профилями.

## Синхронизация и запуск

1. Выбираешь профиль
2. Нажимаешь «Играть»
3. Лаунчер запускает `syncAssets` — скачивает недостающие файлы, проверяет хеши существующих
4. Проходит pre-launch проверка целостности (LaunchGuard)
5. Формируются аргументы запуска (JVM args, classpath, game args)
6. Запускается Java-процесс
7. Включается мониторинг: ProcessGuard следит за DLL-инжектом, RuntimeWatcher — за файлами

Консольный вывод игры доступен на экране Console.
